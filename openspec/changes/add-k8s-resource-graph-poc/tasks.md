## 1. 项目准备

- [x] 1.1 确认概念验证所使用的后端与前端模块的仓库结构
- [x] 1.2 定义 API 响应与前端消费共用的节点和边载荷结构
- [x] 1.3 添加命名空间选择和 Kubernetes 客户端访问所需的初始配置

## 2. 后端图关系提取

- [x] 2.1 实现标准化的 `Node` 和 `Edge` 模型，并支持 `type`、`category`、`reason` 和 `evidence`
- [x] 2.2 为首版支持的资源类型实现资源发现
- [x] 2.3 为支持的资源实现 `owns` 和 `selects` 关系提取规则
- [x] 2.4 为支持的资源实现 `references`、`mounts`、`runs_on` 和 `routes_to` 关系提取规则
- [x] 2.5 添加按命名空间范围查询图的逻辑，返回中心节点及其一跳邻居

## 3. 后端 API

- [x] 3.1 为支持的资源实现命名空间列表和资源搜索接口
- [x] 3.2 为支持的图节点实现节点详情和清单查看接口
- [x] 3.3 实现带边类型筛选能力的邻居查询接口
- [x] 3.4 为支持的关系路径和边方向规则添加测试或校验覆盖

## 4. 前端图体验

- [x] 4.1 实现命名空间选择器和资源搜索入口
- [x] 4.2 实现图画布渲染，支持类型化边样式和节点增量扩展
- [x] 4.3 实现节点详情面板和边关系说明展示
- [x] 4.4 实现边类型筛选，以及针对选中节点和边的图高亮行为

## 5. 概念验证

- [ ] 5.1 在测试命名空间中验证 `Ingress -> Service -> Pod` 的探索流程
- [ ] 5.2 验证 `Pod -> ConfigMap/Secret/PersistentVolumeClaim/Node` 关系的渲染与说明
- [ ] 5.3 审查图的可读性，并调整首版默认的筛选器、布局和详情密度
